# Advantages

### Входные параметры

```js
interface Props {
	items: TAdvantagesItem[];
	classes?: string;
};

type TAdvantagesItem = {
	iconName?: string;
	iconClass?: string;
	title: string;
	text?: string;
	classes?: string;
}
```

### Использование
```js
---
import AdvantagesList from '@/components/Advantages/List.astro';
const advantages = [ 
    {iconName: 'icon-map', iconClass: 'text-accent-500', title: 'Бесплатная доставка', text: 'Автомобиля в официальный сервис*'},
    {iconName: 'safe', iconClass: 'text-accent-500', title: 'Дистанционное согласование', text: 'Дополнительных работ'},
    {iconName: 'chat', iconClass: 'text-accent-500', title: 'Онлайн-подписание', text: 'Документов по итогам проведённого ремонта'}
];
---
```

```astro
<section>
    <div class="container">
        <h2 class="text-xl sm:text-4xl font-medium mb-14 before:hidden text-left mx-0">Преимущества</h2>

        <AdvantagesList items={advantages} classes="grid sm:grid-cols-3 gap-5 lg:gap-10 text-center mb-10 md:mb-20" />

    </div>
</section>
```

<div class="*:!text-left my-10">
    |**Имя параметра**|**По умолчанию**|**Описание**|
    |--|--|--|
	|items|-|Массив элементов `TAdvantagesItem`|
    |classes|`grid sm:grid-cols-3 gap-5 lg:gap-10 text-center mb-10 md:mb-20`|CSS классы для сетки|
</div>

<div class="*:!text-left mb-10">
    ### **TAdvantagesItem**
    |**Имя параметра**|**По умолчанию**|**Описание**|
    |--|--|--|
	|iconName|-|Иконка. Задается имя [Material Design Icons](https://pictogrammers.com/library/mdi/), например, `mdi:car-key`, либо имя svg иконки, расположенной в `src/icons/`|
    |iconClass|-|Классы для иконки, например `text-accent-500`|
    |title|*|Заголовок элемента. **Обязательный**|
    |text|-|Описание для элемента|
    |classes|`bg-gray-100 py-6 lg:py-10 px-2 flex flex-col items-center`|Классы для элемента|
</div>